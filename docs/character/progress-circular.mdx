---
name: Progress Circular
route: /character/progress-circular
menu: Character
edit: false
---

import { Playground, Props } from "docz"
import { ProgressCircleComponent } from "../../src"

# Progress Circle Component

Use this component to display character progressions through a circular progress bar

<Playground>
	<div style={{ display: "flex", alignItems: "center", justifyContent: "space-around" }}>
		<div style={{width: "100px"}}>
    		<ProgressCircleComponent step={1} shape="triangle" progress={0.2} avatar="/public/atom.svg" stroke={{ primary: "#4182b7" }} style={{ margin: "0 20px" }} />
    	</div>
		<div style={{width: "120px"}}>
			<ProgressCircleComponent
				step={7}
				shape="shield"
				progress={0.4}
				avatar="/public/avatar.svg"
				stroke={{ primary: "#a57af7", background: "#6946aa" }}
				style={{ margin: "0 20px" }}
			/>
		</div>
		<div style={{width: "150px"}}>
    		<ProgressCircleComponent step={13} shape="circle" progress={0.55} avatar="/public/bunny.svg" style={{ margin: "0 20px" }} />
    	</div>
		<div style={{width: "120px"}}>
			<ProgressCircleComponent step={42} shape="star" progress={0.7} avatar="/public/book.svg" stroke={{ primary: "#a57af7" }} style={{ margin: "0 20px" }} />
    	</div>
		<div style={{width: "100px"}}>
			<ProgressCircleComponent step={242} shape="square" progress={0.9} avatar="/public/phone.svg" stroke={{ primary: "#fce763" }} style={{ margin: "0 20px" }} />
		</div>
	</div>
    <br />

</Playground>

<br></br>

<Props of={ProgressCircleComponent} />

<Playground>
	{() => {
		const [state, setState] = React.useState({ step: 1, progress: 0, theme: { icon: "circle", primary: "#5dca7a", background: "#d5faee" } })
		const themes = [
			{ icon: "circle", primary: "#5dca7a", background: "#d5faee" },
			{ icon: "triangle", primary: "#2583bc", background: "#d6effe" },
			{ icon: "square", primary: "#ebd10a", background: "#fffad9" },
			{ icon: "star", primary: "#ae72fb", background: "#f4edfe" }
		]
		return (
			<React.Fragment>
				<button
					onClick={() =>
						setState((s) => {
							const p = Math.random()
							const stepUp = p < s.progress
							return { progress: p, step: stepUp ? s.step + 1 : s.step, theme: stepUp ? themes[Math.floor(p*4)] : s.theme }
						})
					}
				>
					Gain Experience
				</button>
				<div style={{width: "200px", marginBottom: "50px"}}>
					<ProgressCircleComponent
						step={state.step}
						shape={state.theme.icon}
						progress={state.progress}
						avatar="/public/bunny.svg"
						stroke={{ primary: state.theme.primary, background: state.theme.background }}
					/>
    			</div>
    		</React.Fragment>
    	)
    }}

</Playground>
